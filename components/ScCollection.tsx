import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const ScCollection = () => {
  return (
    <View>
      <Text style={{color:'white'}} >ScCollection</Text>
    </View>
  )
}

export default ScCollection

const styles = StyleSheet.create({})